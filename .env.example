# ============================================
# RESCONATE - ROOT ENVIRONMENT VARIABLES
# ============================================
# This is a master .env.example file for the entire project
# Copy to .env in the root directory if using a monorepo setup
# Otherwise, use frontend/.env.example or backend/.env.example

# ============================================
# APPLICATION CONFIGURATION
# ============================================
APP_URL=http://localhost:3000
NODE_ENV=development
PORT=3001

# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
# Option 1: Use DATABASE_URL (recommended)
DATABASE_URL=postgresql://user:password@localhost:5432/resconate

# Option 2: Use individual variables (alternative)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=resconate
DB_PASSWORD=your_password_here
DB_PORT=5432

# ============================================
# AUTHENTICATION & SECURITY (REQUIRED)
# ============================================
# Generate secure secrets using: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long_for_production
JWT_EXPIRES_IN=7d
SESSION_SECRET=your_session_secret_key_minimum_32_characters

# ============================================
# EMAIL SERVICE (REQUIRED for email notifications)
# ============================================
# Choose one: sendgrid or mailgun
EMAIL_PROVIDER=sendgrid

# For SendGrid:
SENDGRID_API_KEY=your_sendgrid_api_key_here

# For Mailgun (if using Mailgun instead):
# MAILGUN_API_KEY=your_mailgun_api_key_here
# MAILGUN_DOMAIN=your_mailgun_domain_here

# Email sender configuration
FROM_EMAIL=noreply@resconate.com
FROM_NAME=Resconate
ADMIN_EMAIL=admin@resconate.com

# ============================================
# PAYMENT GATEWAY (REQUIRED for payments)
# ============================================
# Choose one: paystack or flutterwave
PAYMENT_PROVIDER=paystack

# For Paystack:
PAYSTACK_PUBLIC_KEY=your_paystack_public_key_here
PAYSTACK_SECRET_KEY=your_paystack_secret_key_here

# For Flutterwave (if using Flutterwave instead):
# FLUTTERWAVE_PUBLIC_KEY=your_flutterwave_public_key_here
# FLUTTERWAVE_SECRET_KEY=your_flutterwave_secret_key_here

# ============================================
# BANKING INTEGRATION (Uses payment provider)
# ============================================
# Uses same credentials as payment provider
# Choose one: paystack or flutterwave
BANKING_PROVIDER=paystack

# ============================================
# CLOUD STORAGE (OPTIONAL - defaults to local)
# ============================================
# Choose one: local, s3, or cloudinary
STORAGE_TYPE=local

# For AWS S3 (if using S3):
# AWS_ACCESS_KEY_ID=your_aws_access_key_id
# AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
# AWS_REGION=us-east-1
# S3_BUCKET=your_s3_bucket_name

# For Cloudinary (if using Cloudinary):
# CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
# CLOUDINARY_API_KEY=your_cloudinary_api_key
# CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# ============================================
# FILE UPLOAD CONFIGURATION
# ============================================
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760

# ============================================
# PDF GENERATION (OPTIONAL)
# ============================================
PDF_OUTPUT_DIR=./public/pdfs

# ============================================
# WHATSAPP NOTIFICATIONS (OPTIONAL)
# ============================================
# Choose one: twilio or whatsapp_business
WHATSAPP_PROVIDER=twilio

# For Twilio:
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# For WhatsApp Business API:
# WHATSAPP_BUSINESS_API_KEY=your_whatsapp_business_api_key
# WHATSAPP_BUSINESS_PHONE_ID=your_whatsapp_business_phone_id

# ============================================
# SMS NOTIFICATIONS (OPTIONAL)
# ============================================
SMS_PROVIDER=twilio
# TWILIO_PHONE_NUMBER=+1234567890

# ============================================
# CORS CONFIGURATION
# ============================================
CORS_ORIGIN=*

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=300

# ============================================
# NEXT.JS SPECIFIC (Public variables - Frontend only)
# ============================================
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# Use these for client-side accessible variables
NEXT_PUBLIC_WHATSAPP_NUMBER=234XXXXXXXXXX

# ============================================
# QUICK START GUIDE
# ============================================
# 1. Copy this file to .env (or .env.local for Next.js)
# 2. Fill in at minimum:
#    - DATABASE_URL (or DB_* variables)
#    - JWT_SECRET (use a strong random string - see below)
#    - SESSION_SECRET (use a strong random string - see below)
# 3. For full functionality, also configure:
#    - Email service (SendGrid or Mailgun)
#    - Payment gateway (Paystack or Flutterwave)
# 4. Optional services:
#    - Cloud storage (S3 or Cloudinary)
#    - WhatsApp/SMS (Twilio)

# ============================================
# GENERATING SECURE SECRETS
# ============================================
# Run these commands to generate secure secrets:
# 
# Linux/Mac:
#   JWT_SECRET: openssl rand -base64 32
#   SESSION_SECRET: openssl rand -base64 32
# 
# Windows (PowerShell):
#   JWT_SECRET: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
# 
# Node.js:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# ============================================
# SECURITY NOTES
# ============================================
# 1. Never commit .env files to version control
# 2. Use strong, unique secrets in production (minimum 32 characters)
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Use different credentials for development and production
# 5. For Next.js, use .env.local for local development (gitignored)
# 6. Never share .env files or commit them to git
# 7. Use environment-specific .env files (.env.development, .env.production)

# ============================================
# SERVICE SETUP LINKS
# ============================================
# SendGrid: https://sendgrid.com
# Mailgun: https://mailgun.com
# Paystack: https://paystack.com
# Flutterwave: https://flutterwave.com
# AWS S3: https://aws.amazon.com/s3
# Cloudinary: https://cloudinary.com
# Twilio: https://twilio.com
